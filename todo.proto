syntax = "proto3";

message Todo {
    string id = 1;
    string title = 2;
    string todo = 3;
}

service TodoService {
    rpc GetAllTodos (Empty) returns (TodoList) {}
}

message Empty {}

message TodoList {
   repeated Todo todo = 1;
}